---
description: Research and build a grounded character dossier for a novel
argument-hint: <character brief — name, era, profession, setting, role, genre>
---

Use the `character-researcher` subagent to build a structured, well-researched
character dossier for the following character brief:

$ARGUMENTS

If the brief above is empty or too thin to research (no era, place, profession,
or genre to work from), ask me a few quick questions first, then proceed.

Delegate the actual research and writing to the `character-researcher` agent so
it runs with its own focused context. When it's done, surface the saved dossier
path and the headline findings.
